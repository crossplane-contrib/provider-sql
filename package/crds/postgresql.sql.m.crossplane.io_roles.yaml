---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: roles.postgresql.sql.m.crossplane.io
spec:
  group: postgresql.sql.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - sql
    kind: Role
    listKind: RoleList
    plural: roles
    singular: role
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .spec.forProvider.connectionLimit
      name: CONN LIMIT
      type: integer
    - jsonPath: .status.atProvider.privilegesAsClauses
      name: PRIVILEGES
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A Role represents the declarative state of a PostgreSQL role.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A RoleSpec defines the desired state of a Role.
            properties:
              forProvider:
                description: RoleParameters define the desired state of a PostgreSQL
                  role instance.
                properties:
                  configurationParameters:
                    description: |-
                      ConfigurationParameters to be applied to the role. If specified, any other configuration parameters set on the
                      role in the database will be reset.

                      See https://www.postgresql.org/docs/current/runtime-config-client.html for some available configuration parameters.
                    items:
                      description: RoleConfigurationParameter is a role configuration
                        parameter.
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                      type: object
                    type: array
                  connectionLimit:
                    description: ConnectionLimit to be applied to the role.
                    format: int32
                    type: integer
                  passwordSecretRef:
                    description: |-
                      PasswordSecretRef references the secret that contains the password used
                      for this role. If no reference is given, a password will be auto-generated.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  privileges:
                    description: Privileges to be granted.
                    properties:
                      bypassRls:
                        description: BypassRls grants BYPASSRLS when true, allowing
                          the role to bypass row-level security policies.
                        type: boolean
                      createDb:
                        description: CreateDb grants CREATEDB when true, allowing
                          the role to create databases.
                        type: boolean
                      createRole:
                        description: CreateRole grants CREATEROLE when true, allowing
                          this role to create other roles.
                        type: boolean
                      inherit:
                        description: |-
                          Inherit grants INHERIT when true, allowing the role to inherit permissions
                          from other roles it is a member of.
                        type: boolean
                      login:
                        description: Login grants LOGIN when true, allowing the role
                          to login to the server.
                        type: boolean
                      replication:
                        description: Replication grants REPLICATION when true, allowing
                          the role to connect in replication mode.
                        type: boolean
                      superUser:
                        description: SuperUser grants SUPERUSER privilege when true.
                        type: boolean
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A RoleStatus represents the observed state of a Role.
            properties:
              atProvider:
                description: A RoleObservation represents the observed state of a
                  PostgreSQL role.
                properties:
                  configurationParameters:
                    description: ConfigurationParameters represents the applied configuration
                      parameters for the PostgreSQL role.
                    items:
                      description: RoleConfigurationParameter is a role configuration
                        parameter.
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                      type: object
                    type: array
                  privilegesAsClauses:
                    description: |-
                      PrivilegesAsClauses represents the applied privileges state, taking into account
                      any defaults applied by Postgres, and expressed as a list of ROLE PRIVILEGE clauses.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
