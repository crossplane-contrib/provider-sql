---
apiVersion: mssql.sql.m.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: default
  namespace: default
spec:
  credentials:
    source: MSSQLConnectionSecret
    connectionSecretRef:
      namespace: default
      name: db-conn
